#!/bin/bash bash

export PATH=$PATH:$HOME/bin
export PATH=$PATH:$HOME/set-env-gnu
export PATH=$PATH:$HOME/main/bin

# GIT
git config --global user.email "ddiniz@student.42sp.org.br"
git config --global user.name "Diego Diniz"
git config --global core.editor vim
